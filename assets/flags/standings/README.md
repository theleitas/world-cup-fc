Standings card flag assets

These 48 local JPG files are used only inside the standings-card roster grids.
They were generated from FlagCDN/Flagpedia 160px PNG flag images and resized to
144x108 JPG files so the Streamlit app can render consistent, scalable flags
without relying on emoji support or runtime network requests.
